name: CI

on:
    push:
        branches:
            - main
    pull_request:

jobs:
    build-and-test:
        runs-on: ubuntu-latest

        env:
            OTTERAI_USERNAME: ${{ secrets.OTTERAI_USERNAME }}
            OTTERAI_PASSWORD: ${{ secrets.OTTERAI_PASSWORD }}
            TEST_OTTERAI_SPEECH_OTID: ${{ secrets.TEST_OTTERAI_SPEECH_OTID }}

        steps:
            - name: Checkout code
              uses: actions/checkout@v3

            - name: Set up Python
              uses: actions/setup-python@v4
              with:
                  python-version: "3.9"

            - name: Install dependencies
              run: |
                  python -m venv myenv
                  source myenv/bin/activate
                  pip install --upgrade pip
                  pip install .[dev]

            - name: Create .env file
              run: |
                  echo "OTTERAI_USERNAME=${{ secrets.OTTERAI_USERNAME }}" >> .env
                  echo "OTTERAI_PASSWORD=${{ secrets.OTTERAI_PASSWORD }}" >> .env
                  echo "TEST_OTTERAI_SPEECH_OTID=${{ secrets.TEST_OTTERAI_SPEECH_OTID }}" >> .env

            - name: Run pre-commit checks
              env:
                  PRE_COMMIT_HOME: ${{ runner.temp }}/.cache/pre-commit
              run: |
                  source myenv/bin/activate
                  pre-commit install
                  pre-commit run --all-files

            - name: Debug Environment Variables
              run: env | grep OTTERAI_

            - name: Run tests with coverage
              run: |
                  source myenv/bin/activate
                  pytest -s \
                    --cov=otterai \
                    --cov-report=term-missing \
                    --cov-report=lcov:lcov.info \
                    --cov-report=xml:cov.xml

            - name: Upload coverage to Codecov
              uses: codecov/codecov-action@v3
              with:
                  files: lcov.info
                  fail_ci_if_error: true
